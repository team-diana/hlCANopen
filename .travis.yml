language: cpp
compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get install libboost-dev

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - make

script:
  - make test

branches:
  only:
    - master

notifications:
  email: true

os:
  - linux
